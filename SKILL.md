---
name: song-keyboard-jianpu
description: 生成「一字一音」键盘／口风琴／原琴简谱图。把一首歌的歌词逐字配上简谱数字和键盘字母，渲染成蓝色国风竖版 PNG（数字在上、歌词彩色 chip 在中、大写粗体字母套彩色方框在下）。当用户要求做歌的简谱、琴谱、键盘谱、数字谱、光遇/原神琴谱（如"把《XX》做成那样的键盘谱""给XX写个简谱图"）时使用。内含严格的字数/音符数校验和"绝不编造旋律"的查证流程。
---

# 歌曲键盘简谱生成器

把歌词逐字配上简谱数字 + 键盘字母，渲染成竖版 PNG 谱面。

## 铁律：旋律绝不许编

**这是本 skill 最重要的一条。** 不要"根据和弦走向编一个近似的旋律"，不要"按记忆写一个像的版本"。
用户会拿去弹、对着原曲听，编出来的东西一弹就废，比直接说"我需要查证"糟糕得多。

必须先查到真实简谱，并且**至少两个独立来源互相印证**才敢用。

### 查谱策略（按来源可靠度排序）

| 优先级 | 来源类型 | 特点 |
|---|---|---|
| ★★★ | **带八度标注**的文本谱：百度知道（`(1)` 括号表高八度）、米游社/光遇原琴数字谱（`+1` 表高八度） | 最权威，八度信息不丢 |
| ★★ | 简谱网 / 曲谱图片 OCR | 准确但 OCR 易乱码，需人工核对字数 |
| ★ | 搜狗问问 / 贴吧纯数字版（如 `51711`） | **八度信息全丢，且常有音符错位**。只能拿来核对歌词结构和行数，音符要交叉验证 |

搜索关键词模板：
```
<歌名> 简谱 数字简谱 1234567
<歌名> 简谱 "第一句歌词" 音符
<歌名> 琴谱 数字谱 +1 高八度
<歌名> 光遇/原琴 键位谱
```

### 交叉验证流程
1. 每个来源都完整抓下来，逐句对齐歌词
2. 两两对比，**不一致的地方记下来**
3. 冲突时采信规则：
   - 两源一致 → 采信
   - 只有单一来源 → 采信但**在最终回复里明确告诉用户哪几句存疑**
   - 全部冲突 → 优先保留旋律轮廓（升降走向），并告知用户
4. 用 [references/known-sources.md](references/known-sources.md) 里记录的已知错误，避免踩同一个坑

## 工作流

### 1. 整理谱面数据为 JSON

```json
{
  "title": "《歌名》键盘谱",
  "subtitle": "歌手 · 原版简谱 · 一字一音 · 全曲",
  "sections": [
    {"name": "主 歌 一", "rows": [
      {"lyric": "窗外的麻雀在电线杆上多嘴",
       "notes": "5 1^ 7 1^ 1^ 1^ 1^ 7 6 7 6 5"}
    ]}
  ]
}
```

**音符记号**：`3` 中音 · `3^` 高八度 · `_3` 低八度。空格分隔，**必须一个字对一个音**。

### 2. 校验（必做，别跳过）

```bash
python scripts/audit_score.py score.json
```

逐行比对字数 vs 音符数，不一致直接报错退出。中文数错字太容易了，这一步是刚需。

### 3. 渲染

```bash
python scripts/render_score.py score.json -o outputs/song-score.png
```

可选参数：`--width 760`（画布宽）`--cell-max 48`（最大列宽）`--no-footer`（去掉底部山水装饰）

### 4. 交付前自检
- 脚本跑通没报错（字数校验内置在渲染脚本里）
- 抽查渲染结果：标题是蓝的、谱行比背景亮、歌词 chip 有颜色、字母有方框
- 在回复里如实说明**哪几句的八度/音符是推断的**，让用户边弹边听时能定位问题

## 版式说明

三段式对齐，每个字占一个固定宽度的列：

```
    ⑤          ← 简谱数字（上方小圆点=高八度，下方=低八度）
   [ 窗 ]      ← 歌词彩色 chip
   ┌───┐
   │ B │       ← 键盘字母：大写粗体 + 彩色方框
   └───┘
```

**颜色编码**：橙=中音(不按鼠标) · 蓝=高音(右键) · 绿=低音(左键) · 紫=半音(中键)

**默认键位**：`KEY_BY_DEGREE = {1:Z, 2:X, 3:C, 4:V, 5:B, 6:N, 7:M}` —— 键盘最底行七个白键，
配合鼠标键切换八度。字母由**音级**推导，不要逐个手写。

## 已知坑

见 [references/pitfalls.md](references/pitfalls.md)。最容易翻车的三个：
1. **渐变公式写错导致底部泛粉红** —— 写成 `A*(1-t) + 常数` 而不是 `A*(1-t) + B*t`
2. **pip install 装到系统 Python** —— 用隔离的 venv
3. **中文字数数错** —— 必须代码校验，不靠肉眼

## 示例

`examples/qilixiang.json` 是一份完整跑通的参考数据（《七里香》28 行全曲）。
